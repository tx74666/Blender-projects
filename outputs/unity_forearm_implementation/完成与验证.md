# Forearm Correction → Unity：0.58.0

已在 RandomRealm2 安装运行组件，并将 Cosha 的已保存双臂校准导出到 `Assets/Art/Character/Cosha`。当前可用资产是 `Cosha.Runtime.prefab`。

使用方式：Blender 先 Refresh 加载 0.58.0，继续使用 Misc → Unity Export。导出器同时生成 FBX 和 `.forearm.json`；Unity 自动生成／更新 Runtime prefab。动画或 IK 改变手腕姿势时，组件在 LateUpdate 根据实际骨骼计算额外校正。停用或移除组件会恢复原网格。要自行添加游戏组件，请使用自己的 Prefab Variant，避免修改导出器拥有的生成资产。

已有逐圈比例、范围和双侧启用状态会一并传递；校正基于原输入计算，刷新不会累加。使用额外 UV 通道保留细分顶点身份，支持 Unity 导入时的顶点重排和 UV 接缝拆点。保留原有七个表情 Shape Key、骨骼、权重和材质引用。源 X.blend 未改。

实际运行的验证：

- Blender Forearm 导出回归通过；实际 Cosha 的细分映射随机校验误差 1.61×10⁻⁷ 米。
- Blender Export Worker 的八项测试通过，包括移除校准后导出空标记；Export 五项及 UI 五组回归通过。
- Unity 数学对照 687 项通过。
- Unity 合成实际蒙皮检查 11 项通过，包括蒙皮质量、已有 Shape Key、缓存失效、双侧失败恢复、停用、重新启用与过期数据拒绝。
- Cosha 实际 BakeMesh 对照 16 组通过：左右手、正反扭转、抬臂／弯肘及 Root 转向。比较的是开关额外校正后的位移差；最大差异 4.664×10⁻⁷ 米，重复刷新位移差为零。原始网格指纹保持一致。
- 七项导入事务检查通过：重复导入保留 GUID、第二侧非法数据拒绝、数据保存后故障的精确回滚、移除及重复移除、恢复原文件、场景状态不变。
- 实际 sidecar 自动重新导入使 Runtime prefab 和数据资产更新，Prefab GUID `e28dbe21db8321b4e83d7852fb995ccf` 保持不变。MCP 导入调用本身曾超时；通过生成资产更新时间、GUID 和后续 Console 验证实际已完成。
- 优化后再次运行 Animator PlayMode 测试：1 项通过，0 失败。任务 ID `d9830dcf01644d4fb82cb678381bc219`；确认动画更新后同帧的实际网格校正及停用恢复。
- Unity 编译无代码错误；Character Designer、X 副本和 Unity companion 的部署核对全部一致。

性能仅为当前机器 Unity Editor 的诊断：100 次采样中，静止输入中位 0.223 ms／最大 0.387 ms，变化姿势中位 5.714 ms／最大 8.601 ms，两组采样托管分配均为零。未运行 Player Build 或多人角色压力测试。

限制与未验证项：

- 支持已有校准的 ±120° 扭转范围，范围外撤回额外校正并保留普通蒙皮。
- 转移的是额外校正。Unity 普通蒙皮与 Blender 的 Armature 后细分顺序不同，基础外形及法线处理不保证完全一致；未完成最终游戏材质的视觉验收。
- Subdivision 使用导出时固定层级；导出后若更换拓扑或映射，需重新导出。
- 常规 Animator 执行顺序已验证；任意自定义晚执行 IK 脚本未验证，须在其写入骨骼后调用 ApplyNow。
- 当前导出仍为静止角色，未新增动画导出。运行组件可跟随 Unity 内已有动画。
- 原有两个导出警告仍存在：Cosha 两个顶点没有蒙皮权重，Stocking Main 材质需在 Unity 适配。这次没有擅自改权重或材质。
- Blender 运行中的插件尚需用户点击 Refresh；磁盘安装完成不等于当前窗口已重载。

源 X.blend SHA256：`835b4ff666584cf064571121ec2efc41a7286ca422a45f849343b9a7806613ab`。

Unity 已归还：活动场景仍为 `Assets/Scenes/Tools/Terrain Test One.unity`，isDirty=false，Play 已停止，测试结束，最终 Console 查询为 0 条错误／警告。状态资源发布器有缓存延迟；停止状态、活动场景与 Console 另经直接工具核对。本次未保存或修改用户场景。

代码、发布包和部署均为本机修改，未提交或推送 Git。
